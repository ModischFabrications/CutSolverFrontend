export default {
    methods: {
        validQuantity: function (quantity) {
            return (quantity > 0);
        },
        validLength: function (length) {
            return (length > 0);
        }
    }
}
